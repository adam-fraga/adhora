package pages

import (
"github.com/adam-fraga/adhora/views/forms"
)

// Page containing forms dynamically generated from user status (logged in, logged out, etc.)

var (
Login = forms.Login
)

templ Signin() {
<section>
  @Login()
</section>
}
